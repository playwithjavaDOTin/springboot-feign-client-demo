# springboot-feign-client-demo
springboot-feign-client-demo- calling remote Rest API using Feign


Here We have 2 micro-services.

1. employee-service
2. springboot-feign-client

We will call REST from on **employee-service** from **springboot-feign-client** using **fiegn-client**


clone this both repos in local and start is indivisually and call the rest API.

**API** : localhost:9005/employeeRm/remote/getFeignEmployee/1

